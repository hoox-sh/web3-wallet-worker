{
  "name": "web3-wallet-worker",
  "version": "0.0.1",
  "private": true,
  "description": "HOOX web3 wallet worker: ethers.js v6 identity from Secrets Store (private key or BIP-39 mnemonic); telegram + analytics notifications.",
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260708.1",
    "bun-types": "^1.3.14",
    "typescript": "^6.0.3",
    "wrangler": "^4.95.0"
  },
  "dependencies": {
    "@hoox-sh/hoox-shared": "workspace:*",
    "ethers": "^6.16.0"
  },
  "author": "jango-blockchained (hoox-sh)",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hoox-sh/web3-wallet-worker.git"
  }
}
